Django>=5.1,<5.2
djangorestframework>=3.15
polib>=1.2  # compile .po -> .mo without system gettext
gunicorn>=23.0  # production WSGI server
whitenoise>=6.7  # serve static files from the app process
tzdata>=2024.1  # zoneinfo database (slim base images ship without system tz files)
